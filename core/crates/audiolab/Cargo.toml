[package]
name = "audiolab"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Audio library browser, tagger and waveform editor."

[[bin]]
name = "audiolab"
path = "src/main.rs"

[dependencies]
audio-core = { path = "../audio-core" }
catalog = { path = "../catalog" }
indexer = { path = "../indexer" }
server = { path = "../server" }
