[package]
name = "engine"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Real-time audio engine: owns the output device and renders the grain stream block by block."

[dependencies]
fx = { path = "../fx" }
cpal = "0.18"
