[package]
name = "search"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Acoustic fingerprints and similarity search over a library."

[dependencies]
audio-core = { path = "../audio-core" }
