[package]
name = "server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Local HTTP server: serves the UI, streams audio, runs scans."

[dependencies]
audio-core = { path = "../audio-core" }
edit = { path = "../edit" }
fx = { path = "../fx" }
catalog = { path = "../catalog" }
indexer = { path = "../indexer" }
