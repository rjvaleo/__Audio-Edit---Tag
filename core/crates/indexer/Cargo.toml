[package]
name = "indexer"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Walks the audio library, probes and classifies every file, writes the index."

[dependencies]
audio-core = { path = "../audio-core" }
catalog = { path = "../catalog" }
