[package]
name = "catalog"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Library index: classification taxonomy, tag records, TSV storage."

[dependencies]
audio-core = { path = "../audio-core" }
